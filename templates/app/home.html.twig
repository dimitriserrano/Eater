{% extends 'base.html.twig' %}

{% block body %}

<section>
  {% for restaurant in restaurants %}
    <h2>{{ restaurant.name }}</h2>
    <a href="{{ path('product_show', {'id': restaurant.id}) }}" class="btn btn-primary">Voir plus</a>
  {% endfor %}
</section>

{% endblock %}