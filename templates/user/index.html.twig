
{% extends 'base.html.twig' %}

{% block body %}

<div class="jumbotron text-center">
    <h1>{{ user.lastname }}</h1>
    <h1>{{ user.firstname }}</h1>
    <p>{{ user.email }}</p>
    <p>{{ user.address }}, {{ user.city }}</p>
    <p>{{ user.phone }}</p>

</div>

{% endblock %}